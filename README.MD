# 📝 Python CLI To‑Do Manager

A simple Python command‑line To‑Do manager that lets you **add**, **list**, **complete**, and **remove** tasks, with all data stored locally in a JSON file.  
Perfect for practicing **basic Python skills** and **Git version control**.

---

## 🚀 Features
- **Add tasks** with optional priority and due date
- **List tasks** with filters (open/done) and sorting
- **Mark tasks as done**
- **Remove tasks**
- **Persistent storage** in a local JSON file — works offline
- Simple and fast — no external dependencies

---

## 📦 Installation
Clone the repository:
```bash
git clone https://github.com/yourusername/todo-cli.git
cd todo-cli
